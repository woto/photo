.block
  .secondary-navigation
    %ul.wat-cf
      %li.first
        = link_to t("web-app-theme.list"), admin_paper_prices_path
      %li
        = link_to t("web-app-theme.new"), new_admin_paper_price_path
      %li.active
        = link_to t("web-app-theme.edit"), edit_admin_paper_price_path
  .content
    %h2.title
      = t("web-app-theme.edit")
      PaperPrice
    .inner
      - form_for @paper_price, :url => admin_paper_price_path(@paper_price), :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
      -
- content_for :sidebar, render(:partial => 'sidebar')
