.block
  %h3
    Ссылки раздела
  - tabs_tag :namespace => :sidebar, :open_tabs => {:class => "navigation"} do |tab|
    = tab.paper_types  t("site.paper_type.nominative.many"), admin_paper_types_path
    = tab.paper_formats t('site.paper_format.nominative.many'), admin_paper_formats_path
    = tab.printer_types t('site.paper_type.nominative.many') , admin_printer_types_path
    = tab.paper_prices t('site.paper_price.nominative.many'), admin_paper_prices_path
  -
